#!/usr/bin/env bash
#MISE description="Start dev server with Vite (watch + HMR)"
#MISE depends=["setup", "setup-vault", "symlink"]

set -euo pipefail

# Run Vite in dev mode with watch
vite build --watch
