#!/usr/bin/env bash
#MISE description="Watch and rebuild on changes (full dev workflow)"
#MISE depends=["setup-vault", "symlink"]

set -euo pipefail

# Watch for changes (builds on first run and then on file changes)
mise watch build
